# testing docker compose and inter-container comms

to build

    docker build -t aweb .

To start (after `pip install docker-compose`)

    docker-compose up

Then go to localhost:8000 or localhost:8001
